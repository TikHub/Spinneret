// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: spinneret/v1/lease.proto

package spinneretv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/TikHub/Spinneret/gen/go/spinneret/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// LeaseServiceName is the fully-qualified name of the LeaseService service.
	LeaseServiceName = "spinneret.v1.LeaseService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// LeaseServiceAcquireProcedure is the fully-qualified name of the LeaseService's Acquire RPC.
	LeaseServiceAcquireProcedure = "/spinneret.v1.LeaseService/Acquire"
	// LeaseServiceAcquireBatchProcedure is the fully-qualified name of the LeaseService's AcquireBatch
	// RPC.
	LeaseServiceAcquireBatchProcedure = "/spinneret.v1.LeaseService/AcquireBatch"
	// LeaseServiceRenewProcedure is the fully-qualified name of the LeaseService's Renew RPC.
	LeaseServiceRenewProcedure = "/spinneret.v1.LeaseService/Renew"
	// LeaseServiceReleaseProcedure is the fully-qualified name of the LeaseService's Release RPC.
	LeaseServiceReleaseProcedure = "/spinneret.v1.LeaseService/Release"
)

// LeaseServiceClient is a client for the spinneret.v1.LeaseService service.
type LeaseServiceClient interface {
	// Acquire leases one identity for a request against a site endpoint group.
	// Fails with resource_exhausted/no_identity_available,
	// unavailable/circuit_open, unavailable/site_paused or
	// resource_exhausted/no_proxy_available when nothing can be issued within
	// `wait_ms`, and with unavailable/overloaded when the server is at its
	// acquire concurrency limit and shed the call before attempting it.
	Acquire(context.Context, *connect.Request[v1.AcquireRequest]) (*connect.Response[v1.AcquireResponse], error)
	// AcquireBatch leases up to `count` distinct identities in one call. When
	// fewer identities are available the leases that could be issued are
	// returned; an error is returned only when none could be issued.
	AcquireBatch(context.Context, *connect.Request[v1.AcquireBatchRequest]) (*connect.Response[v1.AcquireBatchResponse], error)
	// Renew extends an active lease, bounded by the lease lifetime cap of the
	// rotation policy (lease_lifetime_exceeded when the cap is reached).
	Renew(context.Context, *connect.Request[v1.RenewRequest]) (*connect.Response[v1.RenewResponse], error)
	// Release ends a lease early. Releasing is usually unnecessary: the last
	// report of a lease can set `release: true` instead.
	Release(context.Context, *connect.Request[v1.ReleaseRequest]) (*connect.Response[v1.ReleaseResponse], error)
}

// NewLeaseServiceClient constructs a client for the spinneret.v1.LeaseService service. By default,
// it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and
// sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC()
// or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewLeaseServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) LeaseServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	leaseServiceMethods := v1.File_spinneret_v1_lease_proto.Services().ByName("LeaseService").Methods()
	return &leaseServiceClient{
		acquire: connect.NewClient[v1.AcquireRequest, v1.AcquireResponse](
			httpClient,
			baseURL+LeaseServiceAcquireProcedure,
			connect.WithSchema(leaseServiceMethods.ByName("Acquire")),
			connect.WithClientOptions(opts...),
		),
		acquireBatch: connect.NewClient[v1.AcquireBatchRequest, v1.AcquireBatchResponse](
			httpClient,
			baseURL+LeaseServiceAcquireBatchProcedure,
			connect.WithSchema(leaseServiceMethods.ByName("AcquireBatch")),
			connect.WithClientOptions(opts...),
		),
		renew: connect.NewClient[v1.RenewRequest, v1.RenewResponse](
			httpClient,
			baseURL+LeaseServiceRenewProcedure,
			connect.WithSchema(leaseServiceMethods.ByName("Renew")),
			connect.WithClientOptions(opts...),
		),
		release: connect.NewClient[v1.ReleaseRequest, v1.ReleaseResponse](
			httpClient,
			baseURL+LeaseServiceReleaseProcedure,
			connect.WithSchema(leaseServiceMethods.ByName("Release")),
			connect.WithClientOptions(opts...),
		),
	}
}

// leaseServiceClient implements LeaseServiceClient.
type leaseServiceClient struct {
	acquire      *connect.Client[v1.AcquireRequest, v1.AcquireResponse]
	acquireBatch *connect.Client[v1.AcquireBatchRequest, v1.AcquireBatchResponse]
	renew        *connect.Client[v1.RenewRequest, v1.RenewResponse]
	release      *connect.Client[v1.ReleaseRequest, v1.ReleaseResponse]
}

// Acquire calls spinneret.v1.LeaseService.Acquire.
func (c *leaseServiceClient) Acquire(ctx context.Context, req *connect.Request[v1.AcquireRequest]) (*connect.Response[v1.AcquireResponse], error) {
	return c.acquire.CallUnary(ctx, req)
}

// AcquireBatch calls spinneret.v1.LeaseService.AcquireBatch.
func (c *leaseServiceClient) AcquireBatch(ctx context.Context, req *connect.Request[v1.AcquireBatchRequest]) (*connect.Response[v1.AcquireBatchResponse], error) {
	return c.acquireBatch.CallUnary(ctx, req)
}

// Renew calls spinneret.v1.LeaseService.Renew.
func (c *leaseServiceClient) Renew(ctx context.Context, req *connect.Request[v1.RenewRequest]) (*connect.Response[v1.RenewResponse], error) {
	return c.renew.CallUnary(ctx, req)
}

// Release calls spinneret.v1.LeaseService.Release.
func (c *leaseServiceClient) Release(ctx context.Context, req *connect.Request[v1.ReleaseRequest]) (*connect.Response[v1.ReleaseResponse], error) {
	return c.release.CallUnary(ctx, req)
}

// LeaseServiceHandler is an implementation of the spinneret.v1.LeaseService service.
type LeaseServiceHandler interface {
	// Acquire leases one identity for a request against a site endpoint group.
	// Fails with resource_exhausted/no_identity_available,
	// unavailable/circuit_open, unavailable/site_paused or
	// resource_exhausted/no_proxy_available when nothing can be issued within
	// `wait_ms`, and with unavailable/overloaded when the server is at its
	// acquire concurrency limit and shed the call before attempting it.
	Acquire(context.Context, *connect.Request[v1.AcquireRequest]) (*connect.Response[v1.AcquireResponse], error)
	// AcquireBatch leases up to `count` distinct identities in one call. When
	// fewer identities are available the leases that could be issued are
	// returned; an error is returned only when none could be issued.
	AcquireBatch(context.Context, *connect.Request[v1.AcquireBatchRequest]) (*connect.Response[v1.AcquireBatchResponse], error)
	// Renew extends an active lease, bounded by the lease lifetime cap of the
	// rotation policy (lease_lifetime_exceeded when the cap is reached).
	Renew(context.Context, *connect.Request[v1.RenewRequest]) (*connect.Response[v1.RenewResponse], error)
	// Release ends a lease early. Releasing is usually unnecessary: the last
	// report of a lease can set `release: true` instead.
	Release(context.Context, *connect.Request[v1.ReleaseRequest]) (*connect.Response[v1.ReleaseResponse], error)
}

// NewLeaseServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewLeaseServiceHandler(svc LeaseServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	leaseServiceMethods := v1.File_spinneret_v1_lease_proto.Services().ByName("LeaseService").Methods()
	leaseServiceAcquireHandler := connect.NewUnaryHandler(
		LeaseServiceAcquireProcedure,
		svc.Acquire,
		connect.WithSchema(leaseServiceMethods.ByName("Acquire")),
		connect.WithHandlerOptions(opts...),
	)
	leaseServiceAcquireBatchHandler := connect.NewUnaryHandler(
		LeaseServiceAcquireBatchProcedure,
		svc.AcquireBatch,
		connect.WithSchema(leaseServiceMethods.ByName("AcquireBatch")),
		connect.WithHandlerOptions(opts...),
	)
	leaseServiceRenewHandler := connect.NewUnaryHandler(
		LeaseServiceRenewProcedure,
		svc.Renew,
		connect.WithSchema(leaseServiceMethods.ByName("Renew")),
		connect.WithHandlerOptions(opts...),
	)
	leaseServiceReleaseHandler := connect.NewUnaryHandler(
		LeaseServiceReleaseProcedure,
		svc.Release,
		connect.WithSchema(leaseServiceMethods.ByName("Release")),
		connect.WithHandlerOptions(opts...),
	)
	return "/spinneret.v1.LeaseService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case LeaseServiceAcquireProcedure:
			leaseServiceAcquireHandler.ServeHTTP(w, r)
		case LeaseServiceAcquireBatchProcedure:
			leaseServiceAcquireBatchHandler.ServeHTTP(w, r)
		case LeaseServiceRenewProcedure:
			leaseServiceRenewHandler.ServeHTTP(w, r)
		case LeaseServiceReleaseProcedure:
			leaseServiceReleaseHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedLeaseServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedLeaseServiceHandler struct{}

func (UnimplementedLeaseServiceHandler) Acquire(context.Context, *connect.Request[v1.AcquireRequest]) (*connect.Response[v1.AcquireResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("spinneret.v1.LeaseService.Acquire is not implemented"))
}

func (UnimplementedLeaseServiceHandler) AcquireBatch(context.Context, *connect.Request[v1.AcquireBatchRequest]) (*connect.Response[v1.AcquireBatchResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("spinneret.v1.LeaseService.AcquireBatch is not implemented"))
}

func (UnimplementedLeaseServiceHandler) Renew(context.Context, *connect.Request[v1.RenewRequest]) (*connect.Response[v1.RenewResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("spinneret.v1.LeaseService.Renew is not implemented"))
}

func (UnimplementedLeaseServiceHandler) Release(context.Context, *connect.Request[v1.ReleaseRequest]) (*connect.Response[v1.ReleaseResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("spinneret.v1.LeaseService.Release is not implemented"))
}
