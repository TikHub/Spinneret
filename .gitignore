# Binaries and build output
/.loadtest/
/bin/
/dist/
*.test
*.out
coverage.*

# Local environment
.env
*.local
kek.key
deploy/compose/secrets/

# Local design notes and working documents (not part of the published project).
# The published documentation lives in documents/.
/docs/

# Deployment access: SSH keys, host inventories, anything that gets you onto a
# server. Never in the repository, public or not — a key here is a key in every
# clone, every fork and every backup of them.
/Deploy_SSH/

# IDE
.idea/
.vscode/
.DS_Store

# Web
web/node_modules/
web/dist/*
!web/dist/.keep
web/test-results/
web/playwright-report/

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
*.egg-info/
sdk/python/dist/
.mypy_cache/
.ruff_cache/
