# Spinneret
Spinneret is a control plane for multi-node crawlers and API nodes; it centrally manages identities, proxies, configurations, and keys, and automatically applies cooldowns, bans, and circuit-breaking measures based on request results reported by the nodes.
