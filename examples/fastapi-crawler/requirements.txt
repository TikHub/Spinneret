# Runtime dependencies of the example crawler. The Spinneret SDK is installed from sdk/python
# (see Dockerfile) or with `pip install -e ../../sdk/python` for local development.
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
httpx>=0.27,<1
